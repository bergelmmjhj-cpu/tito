# Server port (optional)
PORT=3000

# Optional Google OAuth (required to enable "Login with Google")
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Leave blank to auto-use <request-origin>/api/auth/google/callback
GOOGLE_REDIRECT_URI=
